import { GetSizeDirective } from './get-size.directive';

describe('GetSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new GetSizeDirective();
    expect(directive).toBeTruthy();
  });
});
