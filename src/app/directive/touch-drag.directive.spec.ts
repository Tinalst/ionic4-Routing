import { TouchDragDirective } from './touch-drag.directive';

describe('TouchDragDirective', () => {
  it('should create an instance', () => {
    const directive = new TouchDragDirective();
    expect(directive).toBeTruthy();
  });
});
